import React, { Component } from 'react'
import { connect } from 'react-redux'

interface Props {

}
interface State {

}

export class ExtendedNav extends Component<Props, State> {
    state = {}

    render() {
        return (
            <div>

            </div>
        )
    }
}

/* const mapStateToProps = (state: ) => ({

})

const mapDispatchToProps = {

}

export default connect(mapStateToProps, mapDispatchToProps)(ExtendedNav) */
